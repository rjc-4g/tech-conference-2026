# AIAD-log

作成日: 2026-06-25

目的: AIAD 検証のため、本プロトタイプで行った作業を作業単位ごとに記録し、各作業での指示内容・GitHub Copilot（参考）トークン消費見積り・作業順序・所要時間・所感を残す。

注: トークン消費は正確計測ではなく、おおまかな範囲見積りです（参考値）。

レポート本文は別ファイルの [AIDD-AIAD-learning-report.md](AIDD-AIAD-learning-report.md) に分離してあります。

---

## 学習レポート：AIDD と AIAD の比較

### 1. 比較の前提
- 対象アプリ: 最小機能の TODO アプリ
- 比較方法: 同一アプリを、AIDD（設計からテストまで AI に一括依頼）と AIAD（チャットで対話しながら手動実装）で開発した場合を比較
- 注意: AIAD は本ログの実測をもとに整理し、AIDD は同規模プロジェクトの一括依頼を想定した概算です

### 2. 比較結果サマリー

| 項目 | AIDD | AIAD |
|---|---|---|
| 作業時間 | 約 2.5〜5 時間（概算） | 約 8.5〜9 時間（実測ベースの集計） |
| 進め方 | 要件整理 → 設計 → 実装 → テストを一気通貫で依頼 | 技術選定 → 骨子作成 → 機能追加を小刻みに反復 |
| トークン消費量 | 約 12,000〜25,000（概算） | 約 7,900〜16,300（本ログの推定合計） |
| 取り組み順序 | 1 回の大きな依頼で全体像を生成し、最後に修正 | 小さな変更を積み上げ、都度確認して進める |
| 感想・気づき | 早く形になるが、依頼の精度が低いと修正コストが増える | 時間はかかるが、意図反映とデバッグがしやすい |

### 3. AIDD の特徴
- 良い点
  - 最初の成果物が早い
  - 設計、実装、テストのつながりを AI がまとめて提案しやすい
  - 人手の切り替えが少なく、全体を短時間で把握しやすい
- 悪い点
  - 依頼時の要件漏れがそのまま実装漏れにつながりやすい
  - 生成結果が大きく、レビューと修正の負荷が高くなりやすい
  - 画面仕様や API 仕様の細かな整合性は、最後に人が詰める必要がある

### 4. AIAD の特徴
- 良い点
  - 変更単位が小さいため、意図と結果を照合しやすい
  - バグ発生箇所を段階的に切り分けられる
  - UI、API、DB、テストを順番に整備できるため、学習効果が高い
- 悪い点
  - 変更回数が増えるため、総作業時間は長くなりやすい
  - 対話を重ねるほどトークン消費が積み上がる
  - 全体最適よりも局所最適に寄りやすく、設計の一貫性維持に注意が必要

### 5. 作業の進め方と順序の比較
- AIDD
  1. 目的と要件をまとめて提示
  2. 設計方針、DB、API、画面、テストをまとめて生成
  3. 生成物をレビューし、必要箇所だけ修正
  4. テストで不足分を補完
- AIAD
  1. 技術選定とプロトタイプ方針を決定
  2. リポジトリ骨子と Docker 化を作成
  3. CRUD、階層 UI、並べ替え、完了ロジックを段階追加
  4. `is_today` や `scheduled_at` のような横断機能を追加
  5. テスト網羅化と不具合修正、最後にドキュメント整備

### 6. 総合評価
- AIDD は「早く形を見たい」局面に強い
- AIAD は「仕様を理解しながら安全に進めたい」局面に強い
- 今回の最小 TODO アプリでは、実装の細部や親子タスクの整合性確認が多かったため、AIAD の方が最終的な制御性は高かった
- 一方で、初期の試作速度だけを見ると AIDD の優位性が高い
- 結論として、短期試作は AIDD、学習と品質確保は AIAD が向いている

---

1) 技術選定とプロトタイプ設計
- 指示: フロント/バックの主要技術を選定し、3時間プロトタイプ計画を提示。
- 使用モデル: Claude 3 Opus
- 推定トークン: 500–1,200
- 作業順序: 1
- 所要時間: 約30分
- 所感: 要件を短時間で固めるために選定基準を明確化できた。

2) リポジトリ骨子と Docker 化のスキャフォールディング
- 指示: Vite フロント、Node/Fastify バック、Docker Compose 構成の雛形を作る。
- 使用モデル: Claude 3 Sonnet
- 推定トークン: 800–1,500
- 作業順序: 2
- 所要時間: 約45分
- 所感: コンテナ化でローカル再現が容易になり、検証サイクルが早まった。

3) 基本 CRUD と親子ツリー UI 実装（フロント中心）
- 指示: タスクの作成・編集・削除・一覧表示、親子入れ子表示、ドラッグ＆ドロップ／並べ替えの初期実装。
- 使用モデル: Claude 3 Sonnet
- 推定トークン: 1,200–3,000
- 作業順序: 3
- 所要時間: 約2時間
- 所感: UI 側の状態同期が複雑になり、API 側の原子操作が必要と判明。

4) `is_today` カラム追加とマイグレーション対応
- 指示: `is_today` フラグを導入し、起動時マイグレーションで既存 DB を更新。PUT/PATCH の欠落パラメータ問題を修正。
- 使用モデル: Claude 3 Haiku
- 推定トークン: 400–900
- 作業順序: 4
- 所要時間: 約30分
- 所感: 実稼働 DB を扱う場合の柱となる作業。早めに対応して正解。

5) 親→子孫の原子操作エンドポイント（set_today / set_complete）実装
- 指示: 再帰 CTE を用いて親配下の子孫をまとめて更新する API を追加（atomic な更新を保証）。
- 使用モデル: Claude 3 Sonnet
- 推定トークン: 600–1,400
- 作業順序: 5
- 所要時間: 約1時間
- 所感: クライアント側で複数 PATCH を投げるより安全で一貫性が取れる設計。

6) 子タスク UX 調整（子の is_today 継承・子に個別『今日やる』非表示 等）
- 指示: 子タスク新規作成時に親の `is_today` を継承、子レベルの『今日やる』非表示に変更。
- 使用モデル: Claude 3 Haiku
- 推定トークン: 300–700
- 作業順序: 6
- 所要時間: 約20–30分
- 所感: 一貫性のある UI に寄与。エンドユーザーの混乱を減らす。

7) 親子の並べ替えと子タスク▲/▼ボタン追加
- 指示: 階層内での並び替えをサポートするため、子タスクに兄弟間移動用の ▲/▼ ボタンを追加。サーバへ `order_num` の一括送信で更新。
- 使用モデル: Claude 3 Haiku
- 推定トークン: 300–800
- 作業順序: 7
- 所要時間: 約25分
- 所感: ドラッグ操作が苦手なユーザー向けの補助として有効。実装はシンプルに保てた。

8) 完了表示・親の完了判定ロジック（子の進捗で親を 100% 扱い）
- 指示: 子タスクが全て 100% のとき親を完了扱いにし、完了ボタンを非表示・灰色表示にする。
- 使用モデル: Claude 3 Haiku
- 推定トークン: 300–700
- 作業順序: 8
- 所要時間: 約20分
- 所感: 視認性が向上し、誤操作を減らせる。

9) ドキュメント作成（AGENTS.md / CLAUDE.md / skill.md / tech-stack-selection.md / design-document.md）
- 指示: タスク完了ワークフローの文書化、エージェント向けスキル説明、技術選定、設計書を追加。
- 使用モデル: Claude 3 Sonnet
- 推定トークン: 600–1,200
- 作業順序: 9
- 所要時間: 約40–60分
- 所感: 作業の棚卸しに有効。自動化ワークフローを運用に組み込める。

10) 実行予定日時（scheduled_at）機能追加・設計書更新・UI改善
- 指示: 
  - DB に `scheduled_at` カラムを追加（マイグレーション対応）
  - API（POST/PUT/PATCH）に `scheduledAt` 必須バリデーション を実装
  - フロント一覧に `実行予定日時` を大きめ表示（`text-lg font-semibold`）
  - 登録・更新フォームに `実行予定日時` 必須入力欄を追加
  - ボタンラベルの改行対策（`whitespace-nowrap` 追加）
  - 設計書に実行予定日時仕様を反映
- 使用モデル: Claude Haiku 4.5
- 推定トークン: 1,300–2,400
- 作業順序: 10
- 所要時間: 約1時間（ポート診断含む）
- 実装箇所:
  - `backend/src/db.js`: `tasks` テーブル定義に `scheduled_at TEXT` 追加
  - `backend/src/index.js`: マイグレーション、バリデーション（isValidDateString）、INSERT/UPDATE/PATCH SQL 修正
  - `frontend/src/App.tsx`: Task 型拡張、formatScheduledAt 関数追加、4箇所での一覧表示追加、全タスクボタン whitespace-nowrap 追加
  - `frontend/src/components/TaskModal.tsx`: scheduledAt state、必須チェック、入力欄追加
  - `design-document.md`: API・DB・画面構成を最新化
  - `docker-compose.yml`: ポート設定変更対応（診断・検証・結局元に戻す）
- 所感: 
  - 既存 DB への透過的なマイグレーションで運用継続性を確保。
  - フロント・バック双方での必須バリデーションで堅牢性向上。
  - ポート競合問題は実運用でのコンテナ管理ノウハウを得る機会になった。
  - ボタン改行は些細ながら UX に実質的な影響。Tailwind CSS の細かい調整の重要性を再認識。

11) バックエンド API テスト網羅化と Jest 実行不具合の解消
- 指示:
  - バックエンド API のテストコードを網羅的に作成
  - テスト実行時のハング/進捗不明問題を解消
  - Docker 実行時の影響有無を切り分け
- 使用モデル: GPT-5.3-Codex
- 推定トークン: 1,500-2,500
- 作業順序: 11
- 所要時間: 約1時間
- 実装箇所:
  - `backend/src/__tests__/api.test.js`: Tasks/Categories/Integration の API テストを再構築
  - `backend/src/app.js`: テスト用 DB 注入を考慮した `createApp` の初期化整理
  - `backend/src/index.js`: `createApp({ logger: true })` 形式へ統一
  - `backend/package.json`: `npm test` を `jest --runInBand --verbose --detectOpenHandles` に更新
  - `backend/jest.config.js`: `testTimeout` 追加
- 主な修正内容:
  - テスト import パス不整合（`../src/app`）を修正（`../app` へ）
  - 破損したテストファイルをクリーン再作成
  - Fastify 初期化完了前リクエストによるエラーを `await app.ready()` で解消
  - 進捗可視化のためテスト開始ログ出力を追加
  - Docker 実行は直接原因ではないことを確認（依存関係再インストール/再ビルド手順を整理）
- 所感:
  - テスト失敗の大半は実装バグではなく、テスト基盤（初期化順・import・実行環境反映）で発生し得る。
  - Fastify は `ready()` を明示するとテスト安定性が大きく向上する。
  - Docker ボリューム構成では `node_modules` の状態差分が再現性に影響するため、環境同期手順の定型化が有効。
  - 進捗ログは「止まって見える」問題の切り分けに非常に有効だった。

- 作業日: 2026-07-29

---

総合所感:
- AIAD 検証では「小さな変更を素早く反復」するフローが有効だった。サーバ側での原子操作とクライアントの UI 一貫性ルールが鍵。
- Copilot（補助 AI）利用のトークンは、実装の粒度や対話量で増減する。設計・API 定義等のテキスト中心作業はトークン効率が良く、実装コードの細かい差分パッチ生成はトークンを多く消費しがち。

更新日: 2026-07-29
