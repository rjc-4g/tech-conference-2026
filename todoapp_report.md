# TODOアプリ開発 実施レポート

## 1. 概要

本レポートでは、AIADの実践として実施したTODOアプリ開発について、実施した工程、作業内容、成果物、所要時間の概算をまとめる。

今回の開発では、ChatGPTを主に設計・実装・テストコード作成に活用し、Claudeを設計書・ソースコード・テストログのレビューに活用した。  
2つのAIを併用することで、実装作業だけでなく、設計の抜け漏れ確認、テスト観点の整理、E2Eテスト失敗時の原因分析までAIADの流れで実施した。

成果物として、Next.js / TypeScript / Prisma / SQLite を用いたTODOアプリを作成し、MVP要件に対する単体・API・E2Eテストまで整備した。

---

## 2. 成果物

成果物は以下である。

- TODOアプリ本体
- Prismaによるデータモデル
- REST API
- TODO一覧画面
- 登録・編集モーダル
- カテゴリ管理機能
- カテゴリフィルタ機能
- 今日やるTODO表示
- 親タスク・子タスクの階層表示
- 検索機能
- 進捗バー
- ドラッグ並べ替え
- 未着手リスク表示
- 着手・完了・完了取消機能
- VitestによるUnit/APIテスト
- PlaywrightによるE2Eテスト
- README・セットアップ手順
- シナリオテスト仕様書

最終的に、Playwright E2Eテストは30件すべて成功した。

---

## 3. 開発工程

### 3.1 要件整理

最初に、TODOアプリとして必要な機能を整理した。

主な要件は以下である。

- TODOの登録・一覧・編集・削除・完了
- 今日やるTODOだけの表示
- 親タスク・子タスクの階層化
- カテゴリ分けとカテゴリごとの表示
- 検索
- 進捗バー
- ドラッグによる順序入れ替え
- タスク登録だけで満足してしまうことを抑止する仕組み

この段階では、MVPとして実装する機能と、将来的に実装する機能を切り分けた。  
認証、複数ユーザー、外部通知、複数タグ、リアルタイム同期、スマホネイティブアプリなどはMVP対象外とした。

### 所要時間

約1.0〜1.5時間

---

### 3.2 基本設計

次に、技術選定、データ設計、API設計、画面設計を行った。

技術構成は以下とした。

- Next.js
- React
- TypeScript
- Tailwind CSS
- SQLite
- Prisma
- dnd kit
- Vitest
- Playwright

データモデルとしては、`Task` と `Category` を定義した。  
`Task` には、タイトル、説明、状態、優先度、期限日、今日対象フラグ、親タスクID、カテゴリID、次の行動、見積時間、並び順、着手日時、最終作業日時、完了日時、論理削除日時などを持たせた。

また、削除は物理削除ではなく `deletedAt` を用いた論理削除とした。  
親タスクに子タスクがある場合は、通常削除ではエラーにし、cascade指定時のみ親子まとめて論理削除する方針とした。

### 所要時間

約2.0〜3.0時間

---

### 3.3 初期実装

設計をもとに、Next.jsプロジェクトの初期構築を行った。

主な作業は以下である。

- Next.js App Router構成の作成
- TypeScript設定
- Tailwind CSS設定
- Prisma設定
- SQLite接続
- `.env.example` と `.gitignore` の整備
- `/api/health` の作成
- READMEへのセットアップ手順記載

この段階で、開発を進めるための土台を整備した。

### 所要時間

約1.0〜1.5時間

---

### 3.4 Prismaスキーマ・DB設計実装

次に、Prismaスキーマを実装した。

主な作業は以下である。

- `Task` モデルの作成
- `Category` モデルの作成
- `TaskStatus` enumの作成
- `Priority` enumの作成
- 親子タスクの自己リレーション定義
- カテゴリとのリレーション定義
- 各種indexの追加
- seedデータの作成

seedデータには、今日対象タスク、期限切れタスク、親子タスク、未着手リスク確認用タスクなどを含めた。

### 所要時間

約1.0〜1.5時間

---

### 3.5 TODO CRUD実装

TODOの基本操作を実装した。

対象機能は以下である。

- TODO登録
- TODO一覧取得
- TODO詳細取得
- TODO編集
- TODO削除
- 完了
- 完了取消
- 着手・作業した操作

APIとしては以下を実装した。

- `GET /api/tasks`
- `POST /api/tasks`
- `GET /api/tasks/:id`
- `PATCH /api/tasks/:id`
- `DELETE /api/tasks/:id`
- `PATCH /api/tasks/:id/complete`
- `PATCH /api/tasks/:id/work`

また、登録・編集時には入力値検証を行い、タイトル、nextAction、見積時間、親タスク、カテゴリなどの妥当性を確認するようにした。

### 所要時間

約3.0〜4.0時間

---

### 3.6 画面実装

一覧画面と登録・編集モーダルを実装した。

主な作業は以下である。

- TODO一覧表示
- 登録モーダル
- 編集モーダル
- 完了チェックボックス
- 着手・作業したボタン
- 削除ボタン
- 優先度表示
- 期限日表示
- カテゴリ表示
- 見積時間表示
- 進捗バー表示
- 未着手リスク表示
- 期限切れ表示
- contextOnly親タスク表示

モーダルについては、ESCキーで閉じる、背景クリックで閉じる、初期フォーカスを当てる、`role="dialog"` を設定するなど、基本的なアクセシビリティ対応も行った。

### 所要時間

約3.0〜4.0時間

---

### 3.7 カテゴリ機能実装

カテゴリ機能として、カテゴリの作成・更新・削除を実装した。

主な作業は以下である。

- カテゴリ一覧取得
- カテゴリ作成
- カテゴリ更新
- カテゴリ削除
- TODO登録・編集時のカテゴリ選択
- タスクカード上のカテゴリ表示
- カテゴリ削除時に該当タスクの `categoryId` を `null` にする処理

その後、要件上「カテゴリごとに見られること」がMVPに含まれることを再確認し、カテゴリフィルタも追加実装した。

カテゴリフィルタでは、`GET /api/tasks?categoryId=...` に対応し、今日フィルタや検索と組み合わせても動作するようにした。

### 所要時間

約2.0〜3.0時間

---

### 3.8 今日フィルタ・階層表示・contextOnly実装

今日やるTODO表示を実装した。

今日対象の条件は以下とした。

- `isToday = true`
- 期限日が今日
- 期限切れかつ未完了

日付判定はAsia/Tokyo基準とした。

また、親子タスクとフィルタの組み合わせでは、子タスクだけが条件に一致した場合に親タスクを `contextOnly = true` として表示する仕様にした。  
これにより、検索や今日フィルタで子タスクだけがヒットしても、親タスクの文脈が分かるようにした。

### 所要時間

約2.0〜3.0時間

---

### 3.9 進捗バー・未着手リスク実装

進捗バーと未着手リスク表示を実装した。

進捗率は以下のルールとした。

- 子タスクがある場合：完了済み子タスク数 / 子タスク総数
- 子タスクがない場合：完了なら100%、未完了なら0%
- 論理削除済み子タスクは計算対象外

未着手リスクは以下の条件で表示することにした。

- 作成から24時間以上経過
- 未完了
- 進捗率0%
- `lastWorkedAt` が未設定

また、「着手」または「作業した」ボタンを押すことで `startedAt` と `lastWorkedAt` を更新し、未着手リスクを解除するようにした。

### 所要時間

約1.5〜2.0時間

---

### 3.10 検索機能実装

検索機能を実装した。

検索対象は以下とした。

- タイトル
- 説明
- nextAction
- カテゴリ名

検索時も、子タスクだけが一致した場合は親タスクを `contextOnly = true` として表示するようにした。  
また、検索条件と今日フィルタ、カテゴリフィルタを組み合わせられるようにした。

### 所要時間

約1.0〜1.5時間

---

### 3.11 ドラッグ並べ替え実装

dnd kitを用いて、タスクのドラッグ並べ替えを実装した。

主な仕様は以下である。

- 同じ親配下でのみ並べ替え可能
- 親子関係はドラッグでは変更しない
- 全体共通の `sortOrder` を更新する
- 今日フィルタや検索中の並べ替えでも、非表示タスクの位置と相対順を維持する
- `contextOnly = true` のタスクはドラッグ対象外とする

APIとして `PATCH /api/tasks/reorder` を実装した。

### 所要時間

約2.0〜3.0時間

---

### 3.12 Unit/APIテスト実装

Vitestを用いて、ロジックとAPIのテストを実装した。

主なテスト対象は以下である。

- 今日判定
- 進捗率計算
- contextOnly表示
- 見積時間バリデーション
- 完了・完了取消
- 着手・作業した操作
- 子タスク完了時の親タスク更新
- reorderロジック
- カテゴリフィルタ

最終的に、Unit/APIテストはすべて成功した。

### 所要時間

約2.0〜3.0時間

---

### 3.13 シナリオテスト仕様書作成

E2E実装前に、シナリオテスト仕様書を作成した。

シナリオはS-01〜S-30まで定義した。  
対象には、基本CRUD、完了、着手、今日フィルタ、階層表示、カテゴリ、検索、ドラッグ並べ替え、論理削除などを含めた。

その後、Claudeによるレビューを通じて以下のような観点を補強した。

- ブラウザネイティブバリデーションの確認方法
- `input:invalid` の利用
- `createdAt` を必要とするテストでPrismaを使う方針
- API/DBで確認すべき項目の切り分け
- カテゴリフィルタをMVP対象に含めること
- E2Eでのstrict mode違反を避けるロケータ方針

### 所要時間

約2.0〜3.0時間

---

### 3.14 Playwright E2Eテスト実装

Playwrightを用いて、MVP要件を確認するE2Eテストを実装した。

主なテスト対象は以下である。

- TODO登録
- 必須入力バリデーション
- 見積時間バリデーション
- TODO編集
- TODO削除
- 子タスクを持つ親タスクの削除
- 完了
- 完了取消
- 着手による未着手リスク解除
- 今日フィルタ
- 期限切れタスクの今日表示
- 親子階層表示
- contextOnly親タスク表示
- カテゴリ作成・更新・削除
- カテゴリフィルタ
- タイトル検索
- 説明検索
- nextAction検索
- カテゴリ名検索
- ドラッグ並べ替え
- フィルタ中の並べ替え
- 論理削除済みタスクの非表示

E2Eテストでは、既存データに依存しないよう、APIまたはPrismaでテストデータを作成した。  
また、画面上に同じ文言が複数存在しても安定して検証できるよう、`data-testid` や一意なタイトルを用いた。

### 所要時間

約4.0〜5.0時間

---

### 3.15 E2E失敗ログの分析と修正

初回のE2E実行では、多数のテストが失敗した。  
ただし原因は大きく以下の4パターンに集約された。

- `getByRole` の部分一致による誤爆
- モーダル外の要素に `getByLabel` が一致する問題
- React制御checkboxに対する `.check()` / `.uncheck()` の不安定さ
- `getByText` の部分一致による複数ヒット

これらに対して以下の修正を行った。

- `exact: true` を追加
- モーダル内操作を `page.getByRole("dialog")` にスコープ
- checkbox操作を `.click()` に変更
- `data-testid` を追加
- 検索・今日フィルタなどのヘルパーを安定化
- CI向けにPlaywrightのretry設定を追加

最終的に、Playwright E2Eテスト30件すべてが成功した。

### 所要時間

約2.0〜3.0時間

---

### 3.16 README・Git管理・コミット

最後に、READMEを更新し、セットアップ手順、テスト実行手順、カテゴリフィルタ仕様、E2Eテスト内容などを整理した。

また、Git管理として以下を確認した。

- `.env` がコミット対象に含まれていないこと
- DBファイルがコミット対象に含まれていないこと
- `node_modules` や `.next` が含まれていないこと
- `package-lock.json` が含まれていること
- テストが成功していること

その後、ローカルブランチでコミットし、GitHubへpushする手順を確認した。

### 所要時間

約0.5〜1.0時間

---

## 4. 所要時間まとめ

実測値ではなく、実施工程と作業量からの概算である。

| 工程 | 概算時間 |
|---|---:|
| 要件整理 | 1.0〜1.5時間 |
| 基本設計 | 2.0〜3.0時間 |
| 初期構築 | 1.0〜1.5時間 |
| Prismaスキーマ・DB設計 | 1.0〜1.5時間 |
| TODO CRUD実装 | 3.0〜4.0時間 |
| 画面実装 | 3.0〜4.0時間 |
| カテゴリ機能実装 | 2.0〜3.0時間 |
| 今日フィルタ・階層表示・contextOnly実装 | 2.0〜3.0時間 |
| 進捗バー・未着手リスク実装 | 1.5〜2.0時間 |
| 検索機能実装 | 1.0〜1.5時間 |
| ドラッグ並べ替え実装 | 2.0〜3.0時間 |
| Unit/APIテスト実装 | 2.0〜3.0時間 |
| シナリオテスト仕様書作成 | 2.0〜3.0時間 |
| Playwright E2Eテスト実装 | 4.0〜5.0時間 |
| E2E失敗ログ分析・修正 | 2.0〜3.0時間 |
| README・Git管理・コミット | 0.5〜1.0時間 |

合計すると、概算で以下となる。

| 区分 | 概算時間 |
|---|---:|
| 実装中心の作業時間 | 約18〜25時間 |
| 設計・レビュー・テスト仕様作成を含む時間 | 約27〜38時間 |
| AIとのやり取り、レビュー往復、ログ確認を含む総所要 | 約30〜40時間 |

---

## 5. AI活用の流れ

今回の開発では、AIを以下のように使い分けた。

### ChatGPT

主な役割は以下である。

- 要件整理
- 設計書作成
- 技術選定
- データモデル設計
- API設計
- ソースコード生成
- テストコード生成
- E2Eテスト修正
- README作成
- Git作業手順の整理

### Claude

主な役割は以下である。

- ChatGPTが出力した設計書のレビュー
- 仕様の抜け漏れ確認
- 実装方針の妥当性確認
- E2Eテスト仕様書のレビュー
- テストログの原因分析
- Playwrightのstrict mode違反やセレクタ問題の指摘

この分担により、1つのAIだけで進めるよりも、レビュー観点を補完しながら開発を進めることができた。

---

## 6. 結果

最終的に、MVP要件として定義したTODOアプリを実装できた。

特に以下の点まで確認できた。

- TODOの基本CRUDが動作する
- 今日やるTODOを表示できる
- 親子タスクを扱える
- カテゴリごとにタスクを表示できる
- 検索できる
- 進捗バーが表示される
- ドラッグで並べ替えできる
- 未着手リスクを表示できる
- 着手・完了操作により作業状態を更新できる
- 論理削除済みタスクが通常表示されない
- Unit/APIテストが成功する
- Playwright E2Eテスト30件がすべて成功する

---

## 7. 振り返り

AIADを用いることで、初学者でも設計から実装、テスト、修正、Git管理まで一連の開発を進められることが確認できた。

一方で、AIへの指示を詳細にしすぎると、やり取りが長くなり、トークン消費が増える傾向があった。  
今回の進め方は品質を担保しやすい反面、効率面では改善余地がある。

次回以降は、以下のような進め方を意識したい。

- 最初に仕様書を固める
- 実装指示は差分中心にする
- AIレビュー結果は要点だけに圧縮して渡す
- 長いログは代表例と共通原因に絞る
- テスト観点は早い段階で決める
- 画面確認とAPI/DB確認の責務を最初から分ける

今回の開発では、AIを実装担当として使うだけでなく、設計レビュー、テスト設計、ログ分析にも活用できた。  
そのため、AIADは単なるコード生成ではなく、開発プロセス全体を支援する手法として有効であると感じた。