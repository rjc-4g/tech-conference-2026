# AGENTS.md

このリポジトリでAIエージェントが作業する際のルールを定義する。

## 最優先ルール

- 作業前に必ず `docs/design.md` と `docs/tech-stack.md` を参照する。
- 設計書にない仕様を推測で実装しない。
- 不明点がある場合は、実装前に質問する。
- 実装変更を行った場合は、原則としてテストを実行する。
- テストを実行できなかった場合は、理由と未検証範囲を記録する。

## 実装ルール

- フロントエンドとバックエンドは分ける。
- 予定ディレクトリは以下とする。
  - `frontend/`
  - `backend/`
  - `docs/`
- UIは一覧画面 + モーダル登録を基本構成とする。
- 既存の設計やテストに反する変更をしない。
- 関係のないリファクタリングを行わない。
- 仕様変更が必要な場合は、先に `docs/design.md` を更新する。

## ライブラリ利用ルール

- 初回インストール時は `latest` を使用する。
- AI側で勝手にバージョン番号を判断して直打ちしない。
- インストール後、`package.json` と lock ファイルに記録されたバージョンを固定版として扱う。
- 実装中に勝手にライブラリを追加・更新しない。
- ライブラリ追加が必要な場合は、理由を説明してユーザ承認を得る。
- 動作確認後、使用した主要ライブラリとバージョンを `docs/tech-stack.md` に記録する。

## テストルール

- バックエンド変更時はAPIテストまたは単体テストを実行する。
- フロントエンド変更時はコンポーネントテストまたはE2Eテストを実行する。
- UI変更時は可能な限り画面キャプチャで確認する。
- 失敗したテストは、内容と対応方針を `docs/aiad-log.md` に記録する。

## AIAD検証ログ

作業単位ごとに `docs/aiad-log.md` を更新する。

記録する内容:

- どんな指示を出したか。
- 何回やり直したか。
- どこで人間レビューが必要だったか。
- テストで何が落ちたか。
- Codex CLIの `/status` で確認できた利用状況。
- 開始前・終了後の使用状況。
- 厳密なトークン数が取得できない場合は、その旨。
- 使用した指示内容と成果物。

## Codex CLI利用状況の記録

Codex CLIの `/status` で確認できる利用状況を、作業開始前と終了後に記録する。

この環境から厳密なトークン数が取得できない場合は、推測値を書かず、以下のように記録する。

```text
厳密なトークン数は取得できなかった。
理由: 実行環境からCodex CLIの /status を直接確認できないため。
```

## レビュー方針

- Phase 3の独自価値機能は、実装前に人間レビューを受ける。
- DBや認証など、設計書にない大きな構成変更は実装前に確認する。