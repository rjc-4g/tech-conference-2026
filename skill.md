# skill.md — エージェント向けドキュメント（自動生成候補）

このファイルはリポジトリ内でエージェントが参照・更新するための簡易スキル説明です。存在しない場合はエージェントが新規作成し、関連ドキュメントの更新候補を記録します。

目的:
- `AGENTS.md` / `CLAUDE.md` に定義される「タスク完了ワークフロー」を補助し、関連ファイルの検出・更新方針を示します。

内容例（テンプレート）:

- 名前: `task-completion-workflow`
- 説明: ユーザーが「タスク完了」と発言した際に実行する必須手順（ドキュメント更新、関連ファイルの列挙、コミット推奨など）を定義します。
- 自動検出パターン: `AGENTS.md`, `CLAUDE.md`, `README.md`, `**/skill.md`, `.gitignore`。
- 推奨アクション:
  1. 変更内容を `AGENTS.md` / `CLAUDE.md` に追記
  2. `.gitignore` 等に必要な注釈を追加
  3. 関連する README や機能別 `skill.md` を更新または新規作成
  4. 変更をコミットする提案をユーザーに出す

このファイルはエージェントが自動生成・更新する候補として置かれています。必要に応じて詳細を編集してください。
